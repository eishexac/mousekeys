return {
  initialSpeed = 100,   -- px/sec on first frame (tap speed)
  acceleration = 5000,  -- px/sec² linear ramp
  maxSpeed = 4000,      -- px/sec cap
  interval = 0.016,
  keys = {
    primary = {
      up = "i",
      down = "k",
      left = "j",
      right = "l",
    },
  },
}
