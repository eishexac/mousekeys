return {
  layerKey = "f18",       -- key that activates mouse mode (hammerspoon key name)
  remapKey = "capslock",  -- physical key to remap to layerKey via hidutil (name or raw HID code)
  tapTimeout = 0.2,       -- max hold duration (seconds) to count as tap-toggle
}
